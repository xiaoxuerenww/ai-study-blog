# AI Study Blog

GitHub Pages blog powered by Jekyll.

## Local preview

```bash
bundle install
bundle exec jekyll serve
```

Then open <http://localhost:4000>.

## Deploy

Push this repo to GitHub as `ai-study-blog.github.io`, then enable GitHub Pages from the repository settings.
